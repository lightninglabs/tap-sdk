# Golang Taproot Assets SDK

The `tap-sdk` repository hosts the Golang client to create Taproot Assets applications. A wrapper for [`tapd`'s gRPC interface](https://lightning.engineering/api-docs/api/taproot-assets).
