# Changelog

`tap-sdk` is pre-v1 and has not made its first public release yet.

Release notes will be maintained here once versioned releases begin. Until
then, breaking changes on `main` are allowed but should be reflected in the
documentation and PR description when they affect the public API.

## Unreleased

- Initial public SDK surface for Taproot Assets v0.8.
